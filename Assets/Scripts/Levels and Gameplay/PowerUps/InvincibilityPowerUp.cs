using UnityEngine;

public class IncincibilityPowerUp : Powerup
{

    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public override void PowerUp()
    {
        opila.Invinciblability();
    }

}
